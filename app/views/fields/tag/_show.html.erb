<%#
# Tag Show Partial
This partial renders an tag attribute
to be displayed on a resource's index page.
By default, the attribute is rendered as a comma seperated list.
## Local variables:
- `field`:
  An instance of Administrate::Field::Tag.
  A wrapper around a tag association pulled from the database.
%>

<%= field.to_s %>
